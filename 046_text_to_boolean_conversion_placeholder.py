# there is no direct way to convert a string/text to boolean in PY, same as JS
# Method like bool(str) is not available in PY
# Manually you have to accept user input, convert to lower case and then compare this lower case input with
# values that represent boolean values like true/false, 0/1 etc.